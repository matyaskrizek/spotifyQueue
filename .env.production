# Loaded automatically by `npm run build` (Vite mode=production).
# Non-secret defaults only — secrets come from the CI environment.
# Deploy can override VITE_WALLS_BAR via the GitHub Actions variable WALLS_BAR.

VITE_WALLS_BAR=westwood
